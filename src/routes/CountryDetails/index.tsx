import { Link } from "react-router-dom";
const CountryDetails = () => {
    return(
        <div>
            country
            <Link to={'/'}>Voltar</Link>
        </div>
    )
}

export default CountryDetails